import styled from 'styled-components';

export const Logo = styled.h1`
  // 로고
  font-size: 1.25rem;
  font-family: 'Raleway', sans-serif;
  font-weight: 300;

  strong {
    font-weight: 600;
  }
`;
